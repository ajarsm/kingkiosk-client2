# Mosquitto configuration

# Listen on all interfaces
listener 1883
listener 9001
protocol websockets

# Allow anonymous access (for demo only - use password in production)
allow_anonymous true

# Log settings
log_dest file /mosquitto/log/mosquitto.log
log_type all
