// A temporary file to check the WebResourceErrorType values available in the package version
import 'package:flutter_inappwebview/flutter_inappwebview.dart';

void main() {
  // Print all the WebResourceErrorType values
  for (var type in WebResourceErrorType.values) {
    print(type.toString());
  }
}
